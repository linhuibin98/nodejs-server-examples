const { resolve } = require('path');

const modelsDir = resolve('src/models');

module.exports = {
    config: `${modelsDir}/config/index.js`,
    'migrations-path': `${modelsDir}/migrate`,
    'seeders-path': `${modelsDir}/seed`,
    'models-path': modelsDir,
};
